<UserControl x:Class="AdHocSensors.WpfApp.SettingsViews.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AdHocSensors.WpfApp.SettingsViews"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0">
        <Grid.Background>
            <SolidColorBrush Color="White" />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer d:DesignHeight="1000"  x:Name="MainScrollViewer" Grid.Row="0" Grid.RowSpan="1" VerticalAlignment="Stretch">
            <StackPanel Orientation="Vertical" Margin="15,10,15,10" HorizontalAlignment="Left">
                <Label Content="test" />
                <Border BorderThickness="1 0 0 0" Margin="20,0,0,0">
                    <StackPanel Margin="5,0,0,0">
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>